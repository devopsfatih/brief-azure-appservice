{
  "name": "techmart-payment-api",
  "version": "1.0.0",
  "description": "TechMart Payment API with Azure integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 0",
    "health": "curl -f http://localhost:3000/health || exit 1",
    "docker:build": "docker build -f Dockerfile.prod -t techmart-api:latest .",
    "docker:dev": "docker build -f Dockerfile.dev -t techmart-api:dev ."
  },
  "dependencies": {
    "express": "^4.18.2",
    "mssql": "^10.0.1",
    "redis": "^4.6.10",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "@azure/identity": "^4.0.1",
    "@azure/keyvault-secrets": "^4.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "payment",
    "api",
    "azure",
    "kubernetes",
    "fintech"
  ],
  "author": "TechMart DevOps Team",
  "license": "MIT"
}